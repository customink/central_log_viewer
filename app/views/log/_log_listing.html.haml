- content_for :head do
  = javascript_include_tag "handlebars"
.iframe#log_div
  %table#log_listing
= javascript_tag do
  var query_url = '#{url_for(:action => "index")}';
  var app_url = '#{url_for(:action => "apps")}';
= javascript_tag do
  $(function() { var grid = new LogViewerHandler(query_url, app_url); });
